<h1>Lists</h1>

<div>
  <div>
    <ol>
      <% @list.each do |list| %>
        <%= link_to list_path(list.id) do %>
          <li><%= list.name %></li>
        <% end %>
      <% end %>
    </ol>
  </div>
<div>
<%= cl_image_tag("yetfxvm1lori8bpokzmr",
      width: 400, height: 300, crop: :fill) %>
</div>

  <div>
    <%= link_to "Create a new list", new_list_path  %>
  </div>
</div>
